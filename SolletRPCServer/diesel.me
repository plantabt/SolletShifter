cargo install diesel_cli --no-default-features --features postgres
cargo install diesel_cli_ext
diesel setup --database-url postgresql://un:pwd@host:port/db
diesel print-schema > src/schema.rs --database-url postgresql://un:pwd@host:port/db
diesel_ext > src/models.rs